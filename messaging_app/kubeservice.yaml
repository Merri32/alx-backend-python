apiVersion: v1
kind: Service
metadata:
  name: django-app-service
spec:
  selector:
    app: django-app-blue   # Change this to "django-app-green" when switching
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
